import { useEffect, useState } from "react";
import { ArrowLeft, Check, EyeOff, Heart, Layers, Menu, Radio, Shield, Smartphone, Waves } from "lucide-react";
import type { PostView, ThemePreference } from "../types";
import { useAppStore } from "../store/useAppStore";
import { getProfile } from "../lib/lemmy";
import { ProfilePostHistory } from "./ProfilePostHistory";

const themeOptions: Array<{ value: ThemePreference; label: string; description: string; colors: [string, string] }> = [
  { value: "forest", label: "Forest", description: "Dark green · light text", colors: ["#081D15", "#E7F2EA"] },
  { value: "pink", label: "Hot pink", description: "Hot pink · black text", colors: ["#FF4FA3", "#160E12"] },
  { value: "blue", label: "Deep blue", description: "Deep blue · white text", colors: ["#082A63", "#F6F9FF"] },
  { value: "tan", label: "Warm tan", description: "Tan · dark red text", colors: ["#D8C39A", "#5B191E"] },
  { value: "matrix", label: "The Matrix", description: "Code black · terminal green", colors: ["#010803", "#63FF75"] },
];

export function UtilityView({ view, onOpenPost }: { view: "profile" | "settings" | "about"; onOpenPost: (post: PostView) => void }) {
  const setView = useAppStore((state) => state.setView);
  const setMenuOpen = useAppStore((state) => state.setMenuOpen);
  const instance = useAppStore((state) => state.instance);
  const token = useAppStore((state) => state.token);
  const logout = useAppStore((state) => state.logout);
  const theme = useAppStore((state) => state.theme);
  const setTheme = useAppStore((state) => state.setTheme);
  const haptics = useAppStore((state) => state.haptics);
  const setHaptics = useAppStore((state) => state.setHaptics);
  const collapseCrossPosts = useAppStore((state) => state.collapseCrossPosts);
  const setCollapseCrossPosts = useAppStore((state) => state.setCollapseCrossPosts);
  const hideVotedPosts = useAppStore((state) => state.hideVotedPosts);
  const setHideVotedPosts = useAppStore((state) => state.setHideVotedPosts);
  const [profile, setProfile] = useState<{ person: { id: number; name: string; display_name?: string; avatar?: string }; local_user: { email?: string } } | null>(null);

  useEffect(() => {
    if (view !== "profile" || !token) return;
    getProfile(instance, token).then((result) => setProfile(result ?? null)).catch(() => setProfile(null));
  }, [view, instance, token]);

  const titles = { profile: "Profile", settings: "Settings", about: "About" };
  return (
    <main className="min-h-[100dvh]">
      <header className="safe-top sticky top-0 z-20 border-b border-line/70 bg-canvas/80 px-4 pb-3 backdrop-blur-xl"><div className="mx-auto flex max-w-xl items-center justify-between"><button type="button" onClick={() => setView("feed")} className="rounded-full p-2.5 text-muted hover:bg-panel hover:text-ink" aria-label="Back to feed"><ArrowLeft className="h-5 w-5" /></button><h1 className="font-display text-2xl">{titles[view]}</h1><button type="button" onClick={() => setMenuOpen(true)} className="rounded-full p-2.5 text-muted hover:bg-panel hover:text-ink" aria-label="Open menu"><Menu className="h-5 w-5" /></button></div></header>
      <section className="mx-auto max-w-xl px-5 py-8 safe-bottom">
        {view === "profile" && <div><div className="rounded-[2rem] bg-ink p-6 text-panel shadow-card">{profile?.person.avatar ? <img src={profile.person.avatar} alt="" className="h-16 w-16 rounded-full object-cover" /> : <span className="grid h-16 w-16 place-items-center rounded-full bg-panel/10 text-2xl font-display">{profile?.person.name?.slice(0, 1).toUpperCase() ?? "S"}</span>}<h2 className="mt-5 font-display text-4xl">{profile?.person.display_name || profile?.person.name || (token ? "Connected human" : "Quiet observer")}</h2><p className="mt-2 text-sm text-panel/65">{profile?.person.name ? `@${profile.person.name}${profile.local_user.email ? ` · ${profile.local_user.email}` : ""}` : token ? "Your identity stays with your Lemmy instance." : "You’re browsing in read-only mode."}</p><div className="mt-6 rounded-2xl bg-panel/10 p-4"><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-panel/50">Home instance</p><p className="mt-1 font-extrabold uppercase tracking-[.08em]">{instance}</p></div></div><ProfilePostHistory personId={profile?.person.id} onOpenPost={onOpenPost} /><button type="button" onClick={logout} className="mt-4 w-full rounded-2xl border border-line bg-panel py-3.5 text-sm font-extrabold text-flare">{token ? "Log out" : "Change instance"}</button></div>}
        {view === "settings" && <div className="space-y-5"><section className="rounded-[1.5rem] border border-line bg-panel p-5 shadow-soft"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-canvas"><Smartphone className="h-5 w-5" /></span><div><h2 className="font-extrabold">Color theme</h2><p className="text-xs text-muted">Choose the palette for The Human Network.</p></div></div><div className="mt-5 grid grid-cols-2 gap-2">{themeOptions.map((option) => <button key={option.value} type="button" onClick={() => setTheme(option.value)} aria-pressed={theme === option.value} className={`relative rounded-2xl border p-3 text-left transition active:scale-[.98] ${theme === option.value ? "border-ink bg-canvas text-ink" : "border-line bg-canvas/60 text-muted hover:border-ink/50 hover:text-ink"}`}><span className="flex items-center gap-2"><span className="flex h-8 w-12 overflow-hidden rounded-full border border-line/60" aria-hidden="true"><span className="h-full flex-1" style={{ backgroundColor: option.colors[0] }} /><span className="h-full flex-1" style={{ backgroundColor: option.colors[1] }} /></span>{theme === option.value && <span className="ml-auto grid h-6 w-6 place-items-center rounded-full bg-ink text-panel"><Check className="h-3.5 w-3.5" /></span>}</span><span className="mt-3 block text-xs font-extrabold text-ink">{option.label}</span><span className="mt-0.5 block text-[10px] font-semibold leading-snug">{option.description}</span></button>)}</div></section><section className="flex items-center justify-between rounded-[1.5rem] border border-line bg-panel p-5 shadow-soft"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-canvas"><Waves className="h-5 w-5" /></span><div><h2 className="font-extrabold">Haptic taps</h2><p className="text-xs text-muted">A tiny pulse on committed swipes.</p></div></div><button type="button" onClick={() => setHaptics(!haptics)} aria-label="Toggle haptics" aria-pressed={haptics} className={`relative h-7 w-12 rounded-full transition ${haptics ? "bg-sprout" : "bg-line"}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition-transform ${haptics ? "translate-x-5" : "translate-x-1"}`} /></button></section><section className="flex items-center justify-between rounded-[1.5rem] border border-line bg-panel p-5 shadow-soft"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-canvas"><Layers className="h-5 w-5" /></span><div><h2 className="font-extrabold">Collapse cross-posts</h2><p className="text-xs text-muted">Group the same link posted to several communities into one card.</p></div></div><button type="button" onClick={() => setCollapseCrossPosts(!collapseCrossPosts)} aria-label="Toggle cross-post collapsing" aria-pressed={collapseCrossPosts} className={`relative h-7 w-12 shrink-0 rounded-full transition ${collapseCrossPosts ? "bg-sprout" : "bg-line"}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition-transform ${collapseCrossPosts ? "translate-x-5" : "translate-x-1"}`} /></button></section>{token && <section className="flex items-center justify-between rounded-[1.5rem] border border-line bg-panel p-5 shadow-soft"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-canvas"><EyeOff className="h-5 w-5" /></span><div><h2 className="font-extrabold">Hide voted posts</h2><p className="text-xs text-muted">Skip posts you already upvoted or downvoted, even before they’re marked read.</p></div></div><button type="button" onClick={() => setHideVotedPosts(!hideVotedPosts)} aria-label="Toggle hiding voted posts" aria-pressed={hideVotedPosts} className={`relative h-7 w-12 shrink-0 rounded-full transition ${hideVotedPosts ? "bg-sprout" : "bg-line"}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition-transform ${hideVotedPosts ? "translate-x-5" : "translate-x-1"}`} /></button></section>}</div>}
        {view === "about" && <div><p className="text-[10px] font-extrabold uppercase tracking-[.2em] text-sprout">A slower social ritual</p><h2 className="mt-2 font-display text-5xl leading-[.95] tracking-[-.04em]">Less feed.<br /><em>More feeling.</em></h2><p className="mt-6 text-sm leading-7 text-muted">The Human Network is a focused window into Lemmy: one post, one choice, no algorithm pretending to know your soul. Your instance, credentials, saves, and preferences stay in your browser.</p><div className="mt-8 grid gap-3"><div className="flex gap-3 rounded-2xl bg-panel p-4 shadow-soft"><Radio className="h-5 w-5 text-sprout" /><div><h3 className="text-sm font-extrabold">Federated by design</h3><p className="mt-1 text-xs leading-relaxed text-muted">Connect to the Lemmy community you already call home.</p></div></div><div className="flex gap-3 rounded-2xl bg-panel p-4 shadow-soft"><Shield className="h-5 w-5 text-sprout" /><div><h3 className="text-sm font-extrabold">Private by default</h3><p className="mt-1 text-xs leading-relaxed text-muted">No tracking account, no server-side profile, no data brokerage.</p></div></div><a href="https://liberapay.com/davymellado/donate" target="_blank" rel="noreferrer" className="flex gap-3 rounded-2xl border border-line bg-panel p-4 shadow-soft transition hover:border-flare/50 hover:text-flare" aria-label="Donate to The Human Network creator Davy Mellado on Liberapay"><Heart className="h-5 w-5 text-flare" /><div><h3 className="text-sm font-extrabold">Support the creator</h3><p className="mt-1 text-xs leading-relaxed text-muted">Donate to Davy Mellado to support the continued creation of The Human Network.</p></div></a></div><p className="mt-8 text-xs font-bold text-muted">The Human Network 1.0 · Made for the open social web.</p></div>}
      </section>
    </main>
  );
}
